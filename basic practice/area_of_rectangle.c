#include <stdio.h>

int main(){
    int l,b,area;
    printf("Enter the length and breath of Rectangle ");
    scanf("%d,%d",&l,&b);
    area=l*b;
    printf("The Area of the Rectangle is %d",area);
    return 0;
}